## MySQL Master and Slave Dockerfile repo

This folder contains the dockerfiles to build MySQL master image and slave image

This is done to ensure data is replicated from master to slave to enable HA